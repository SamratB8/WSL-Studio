namespace WslStudio.App.ViewModels;

public sealed partial class SettingsViewModel()
    : PageViewModelBase(
        "Settings",
        "Application preferences will appear here as the shell gains configurable behavior.")
{
}
