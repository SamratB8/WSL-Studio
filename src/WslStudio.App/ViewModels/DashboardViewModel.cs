namespace WslStudio.App.ViewModels;

public sealed partial class DashboardViewModel()
    : PageViewModelBase(
        "Dashboard",
        "A focused starting point for managing WSL from a native Windows interface.")
{
}
