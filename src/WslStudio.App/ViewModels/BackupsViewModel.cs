namespace WslStudio.App.ViewModels;

public sealed partial class BackupsViewModel()
    : PageViewModelBase(
        "Backups",
        "Export and recovery workflows will be designed around explicit user control and safe defaults.")
{
}
