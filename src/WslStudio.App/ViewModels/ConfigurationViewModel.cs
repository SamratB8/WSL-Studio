namespace WslStudio.App.ViewModels;

public sealed partial class ConfigurationViewModel()
    : PageViewModelBase(
        "Configuration",
        "Supported WSL configuration workflows will be presented with validation and clear review steps.")
{
}
