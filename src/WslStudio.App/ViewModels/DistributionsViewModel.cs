namespace WslStudio.App.ViewModels;

public sealed partial class DistributionsViewModel()
    : PageViewModelBase(
        "Distributions",
        "Installed distributions and lifecycle actions will appear here when WSL integration is added.")
{
}
