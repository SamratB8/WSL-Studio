<Page
    x:Class="WslStudio.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WslStudio.App.Controls">
    <ScrollViewer>
        <Grid
            MaxWidth="1180"
            Padding="32"
            HorizontalAlignment="Stretch"
            RowSpacing="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Spacing="8">
                <TextBlock Style="{StaticResource PageTitleTextBlockStyle}" Text="{Binding Title}" />
                <TextBlock Style="{StaticResource PageSubtitleTextBlockStyle}" Text="{Binding Subtitle}" />
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                <Grid ColumnSpacing="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Width="48"
                        Height="48"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        CornerRadius="8">
                        <SymbolIcon
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Symbol="Home" />
                    </Border>

                    <StackPanel Grid.Column="1" Spacing="6">
                        <TextBlock Style="{StaticResource SectionTitleTextBlockStyle}" Text="Management workspace coming soon" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="This shell is ready for future WSL integration. No distributions, commands, or system data are loaded in this preview."
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2" ColumnSpacing="16" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <controls:InfoCard
                    Title="Distribution overview"
                    Description="Installed distributions, status, and version details will appear here after discovery support is added."
                    Icon="Library" />
                <controls:InfoCard
                    Grid.Column="1"
                    Title="Configuration review"
                    Description="Supported settings will be presented with validation before changes are applied."
                    Icon="Setting" />
                <controls:InfoCard
                    Grid.Row="1"
                    Title="Backup workflows"
                    Description="Export-focused flows will provide clear review steps before destructive actions."
                    Icon="Save" />
                <controls:InfoCard
                    Grid.Row="1"
                    Grid.Column="1"
                    Title="Diagnostics"
                    Description="Operation results and troubleshooting details will be available without collecting unnecessary data."
                    Icon="Find" />
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
